# WebSocket Connection-header map for the Grafana auth proxy.
# Mounted as /etc/nginx/conf.d/00-map.conf so it is included at http {}
# scope before default.conf (nginx includes conf.d/*.conf alphabetically).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
